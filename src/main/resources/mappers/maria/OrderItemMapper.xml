<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.goorm.board.mapper.OrderItemMapper">

    <!-- 발주 상품 ResultMap -->
    <resultMap id="orderItemResultMap" type="io.goorm.board.entity.OrderItem">
        <id property="orderItemSeq" column="order_item_seq"/>
        <result property="orderSeq" column="order_seq"/>
        <result property="productSeq" column="product_seq"/>
        <result property="quantity" column="quantity"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="discountRate" column="discount_rate"/>
        <result property="discountAmount" column="discount_amount"/>
        <result property="lineTotal" column="line_total"/>
        <result property="createdAt" column="created_at"/>
        <!-- 조인 필드 -->
        <result property="productName" column="product_name"/>
        <result property="productCode" column="product_code"/>
        <result property="categoryName" column="category_name"/>
    </resultMap>

    <!-- 공통 조인 구문 -->
    <sql id="joinTables">
        LEFT JOIN products p ON oi.product_seq = p.product_seq
        LEFT JOIN categories c ON p.category_seq = c.category_seq
    </sql>

    <!-- 공통 select 컬럼 -->
    <sql id="selectColumns">
        oi.order_item_seq, oi.order_seq, oi.product_seq, oi.quantity,
        oi.unit_price, oi.discount_rate, oi.discount_amount, oi.line_total, oi.created_at,
        p.name as product_name, p.code as product_code, c.name as category_name
    </sql>

    <!-- 발주 상품 등록 -->
    <insert id="insert" parameterType="io.goorm.board.entity.OrderItem" useGeneratedKeys="true" keyProperty="orderItemSeq">
        INSERT INTO order_items (
            order_seq, product_seq, quantity, unit_price,
            discount_rate, discount_amount, line_total
        ) VALUES (
            #{orderSeq}, #{productSeq}, #{quantity}, #{unitPrice},
            #{discountRate}, #{discountAmount}, #{lineTotal}
        )
    </insert>

    <!-- 발주 상품 일괄 등록 -->
    <insert id="insertBatch" parameterType="list">
        INSERT INTO order_items (
            order_seq, product_seq, quantity, unit_price,
            discount_rate, discount_amount, line_total
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderSeq}, #{item.productSeq}, #{item.quantity}, #{item.unitPrice},
             #{item.discountRate}, #{item.discountAmount}, #{item.lineTotal})
        </foreach>
    </insert>

    <!-- 발주 상품 수정 -->
    <update id="update" parameterType="io.goorm.board.entity.OrderItem">
        UPDATE order_items SET
            quantity = #{quantity},
            unit_price = #{unitPrice},
            discount_rate = #{discountRate},
            discount_amount = #{discountAmount},
            line_total = #{lineTotal}
        WHERE order_item_seq = #{orderItemSeq}
    </update>

    <!-- 발주 상품 삭제 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM order_items WHERE order_item_seq = #{orderItemSeq}
    </delete>

    <!-- 발주별 상품 목록 조회 -->
    <select id="findByOrderSeq" parameterType="long" resultMap="orderItemResultMap">
        SELECT <include refid="selectColumns"/>
        FROM order_items oi
        <include refid="joinTables"/>
        WHERE oi.order_seq = #{orderSeq}
        ORDER BY oi.order_item_seq
    </select>

    <!-- 발주별 상품 삭제 -->
    <delete id="deleteByOrderSeq" parameterType="long">
        DELETE FROM order_items WHERE order_seq = #{orderSeq}
    </delete>

</mapper>