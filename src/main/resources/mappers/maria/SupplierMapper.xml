<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.goorm.board.mapper.SupplierMapper">

    <!-- SupplierDto ResultMap -->
    <resultMap id="supplierResultMap" type="io.goorm.board.dto.SupplierDto">
        <id property="supplierSeq" column="supplier_seq"/>
        <result property="name" column="name"/>
        <result property="contactPerson" column="contact_person"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="address" column="address"/>
        <result property="description" column="description"/>
        <result property="isActive" column="is_active"/>
    </resultMap>

    <!-- 활성 공급업체 목록 조회 -->
    <select id="findAllActive" resultMap="supplierResultMap">
        SELECT
            supplier_seq,
            name,
            contact_person,
            email,
            phone,
            address,
            description,
            is_active
        FROM suppliers
        WHERE is_active = TRUE
        ORDER BY name ASC
    </select>

    <!-- 수정용 공급업체 목록 조회 (활성 + 현재 선택된 공급업체) -->
    <select id="findAllActiveOrSelected" parameterType="long" resultMap="supplierResultMap">
        SELECT
            supplier_seq,
            name,
            contact_person,
            email,
            phone,
            address,
            description,
            is_active
        FROM suppliers
        WHERE is_active = TRUE
        <if test="selectedSupplierSeq != null">
           OR supplier_seq = #{selectedSupplierSeq}
        </if>
        ORDER BY name ASC
    </select>

    <!-- 공급업체 상세 조회 -->
    <select id="findBySeq" parameterType="long" resultMap="supplierResultMap">
        SELECT
            supplier_seq,
            name,
            contact_person,
            email,
            phone,
            address,
            description,
            is_active
        FROM suppliers
        WHERE supplier_seq = #{supplierSeq}
    </select>

</mapper>