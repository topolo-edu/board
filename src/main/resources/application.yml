spring:
  # H2 ?????? ??
  h2:
    console:
      enabled: true
      path: /h2-console

  # ????? ?? (H2 ??? DB)
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  # JPA ??
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

  # Thymeleaf ??
  thymeleaf:
    cache: false